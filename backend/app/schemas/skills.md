# backend/app/schemas

This package contains Pydantic schemas for request and response validation.

## Purpose

- Define typed data shapes for API input and output.
- Validate incoming payloads and serialize outgoing data.

## Notes

- Keep schema definitions clean and focused on API contract.
- Use schemas to separate external API formats from internal ORM models.
