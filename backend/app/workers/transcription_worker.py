from uuid import UUID


def process_transcription_job(job_id: UUID) -> None:
    """Placeholder for the queued worker entrypoint."""
    raise NotImplementedError("Background job processing will be wired in the next pass.")
